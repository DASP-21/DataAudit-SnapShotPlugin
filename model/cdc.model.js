const mongoose = require('mongoose');
const {Schema} = mongoose;

let cdcSchema = new Schema({
    // capture format will go here.
   
});


module.exports = mongoose.model('CDC', cdcSchema);